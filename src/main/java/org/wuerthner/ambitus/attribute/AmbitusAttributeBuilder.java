package org.wuerthner.ambitus.attribute;

import org.wuerthner.cwn.api.TimeSignature;
import org.wuerthner.sport.attribute.IntegerAttribute;

public class AmbitusAttributeBuilder {/*extends AttributeBuilder {
   public AmbitusAttributeBuilder(String id) {
        super(id);
    }

    public AmbitusAttributeBuilder required() {
        super.required();
        return this;
    }

    public AmbitusAttributeBuilder label(String label) {
        super.label(label);
        return this;
    }

    public AmbitusAttributeBuilder defaultValue(Object defaultValue) {
        super.defaultValue(defaultValue);
        return this;
    }

    public TimeSignatureAttribute buildTimeSignatureAttribute() {
        return new TimeSignatureAttribute(id, label, (TimeSignature) defaultValue, readonly, required, hidden, description, dependencies, validators);
    }

    public PositionAttribute buildPositionAttribute() {
        return new PositionAttribute(id, label, (Long) defaultValue, readonly, required, hidden, description, dependencies, validators);
    }

    public PitchAttribute buildPitchAttribute() {
        return new PitchAttribute(id, label, (Integer) defaultValue, readonly, required, hidden, description, dependencies, validators);
    }

    */
}
